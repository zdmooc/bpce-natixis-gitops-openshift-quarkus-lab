# Backend Quarkus — Kafka

État debug :
- service `kafka` “fake” utilisé pour DNS
- `KAFKA_BOOTSTRAP_SERVERS=kafka:9092`

Cible propre (Strimzi) :
- `KAFKA_BOOTSTRAP_SERVERS=bpce-kafka-kafka-bootstrap.bpce-platform.svc:9092`
