apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  template:
    spec:
      containers:
      - name: backend
        image: default-route-openshift-image-registry.apps-crc.testing/bpce-prod/container-tools-api:prod
